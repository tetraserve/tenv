version: '3.8'

services:
  snn:
    restart: always
    image: "yuknak/snn:latest"
    env_file:
      - ../config/snn.env
    expose:
      - "3001"
    labels:
      traefik.enable: true
      traefik.frontend.rule: "Host:test.<%= @config['app']['domain'] %>"
      #traefik.frontend.auth.basic: '<%= @config['pma']['basic_auth'] %>'
      traefik.port: 3001
